package com.example.mysynergybot.telegramchat.thread;

import lombok.extern.slf4j.Slf4j;


@Slf4j
public class MessageReceiver implements Runnable {

//    private final int WAIT_FOR_NEW_MESSAGE_DELAY = 1000;
//
//
//
//    @Override
   public void run() {
//        log.info("[STARTED] MsgReciever...");
//        while (true) {
//            for (Object object = TelegramBotChat.receiveQueue.poll(); object != null; object = TelegramBotChat.receiveQueue.poll()) {
//                log.debug("New object for analyze in queue " + object.toString());
//                analyze(object);
//            }
//            try {
//                Thread.sleep(WAIT_FOR_NEW_MESSAGE_DELAY);
//            } catch (InterruptedException e) {
//                log.error("Catch interrupt. Exit", e);
//                return;
//            }
//        }
//
 }
//    private void analyze(Object object) {
//        if (object instanceof Update) {
//            Update update = (Update) object;
//            //some logic to handle Update object
//            log.debug("Update recieved: " + update.toString());
//        }
//
//        else log.warn("Cant operate type of object: " + object.toString());
//    }
}
